/* Speed-drill question pool: scenario phrase -> AI-engineering technique reflex training.
 * 48 items covering the AI Engineer blueprint. Plain-text q/options; answer is the index into options. */
window.COURSE.registerDrills([
  { q: "Model cannot count the letters in a single word", options: ["Tokenization","Low temperature","Small context window","Quantization error"], answer: 0, why: "Models see subword <strong>tokens</strong>, not characters, so letter-level tasks fail; temperature and context size have nothing to do with how text is segmented." },
  { q: "Same paragraph costs three times more tokens in another language", options: ["Prompt caching miss","Tokenizer vocabulary coverage","Higher output temperature","Rate limiting"], answer: 1, why: "Tokenizers trained mostly on English split other scripts into many more tokens per word, inflating cost and latency; caching and sampling do not change token counts." },
  { q: "Extraction pipeline returns slightly different JSON on every run", options: ["Add a reranker","Lower the temperature toward zero","Increase max tokens","Fine-tune the model"], answer: 1, why: "Temperature near zero makes sampling near-greedy and outputs far more repeatable; deterministic-style tasks should not use creative sampling settings." },
  { q: "Brainstorming output is repetitive, ten near-identical ideas", options: ["Raise temperature or top-p","Enable prompt caching","Shrink the context window","Add stop sequences"], answer: 0, why: "Higher temperature and top-p widen the sampling distribution for diverse output; caching and stop sequences affect cost and truncation, not variety." },
  { q: "Task works only after showing a few worked examples in the prompt", options: ["Few-shot prompting","Retrieval-augmented generation","Speculative decoding","Constrained decoding"], answer: 0, why: "Few-shot examples demonstrate the input-output pattern in context; RAG injects knowledge, not task demonstrations." },
  { q: "Multi-step reasoning answer improves when model works through steps before concluding", options: ["Greedy decoding","Chain-of-thought prompting","Prompt compression","Logit bias"], answer: 1, why: "Chain-of-thought lets the model spend tokens on intermediate reasoning before the final answer, sharply improving multi-step accuracy; greedy decoding changes nothing about reasoning." },
  { q: "Huge identical system prompt re-billed at full price on every request", options: ["Model routing","Output token capping","Prompt caching","Batch inference"], answer: 2, why: "Prompt caching stores the processed static prefix so repeated requests pay a heavily discounted rate; routing changes which model runs, not what the prefix costs." },
  { q: "Cache never hits because a timestamp sits at the top of the prompt", options: ["Put dynamic content after the static prefix","Increase the cache TTL","Hash the prompt before sending","Use a smaller model"], answer: 0, why: "Caches match on an exact stable prefix, so any changing byte near the top invalidates everything after it; order prompts static-first, dynamic-last." },
  { q: "Model invents fields and parameters your parser never defined", options: ["Schema-constrained structured outputs","Higher temperature","Longer system prompt","Semantic caching"], answer: 0, why: "Enforcing a JSON schema via structured outputs or constrained decoding makes invalid shapes impossible; prompt wording alone only lowers, never eliminates, the failure rate." },
  { q: "Model narrates calling your API in prose instead of actually invoking it", options: ["Native tool calling with defined schemas","Few-shot examples of API prose","Retry with backoff","Streaming responses"], answer: 0, why: "Declaring tools with typed schemas lets the model emit machine-readable tool calls the runtime executes; asking nicely in prose leaves you parsing free text." },
  { q: "Agent keeps picking the wrong tool from a list of twenty", options: ["Raise the step budget","Clearer tool names and descriptions","Increase temperature","Add a vector database"], answer: 1, why: "Tool selection is driven almost entirely by names and descriptions in context; cryptic or overlapping descriptions are the top cause of wrong-tool calls." },
  { q: "One do-everything tool with twelve optional parameters confuses the model", options: ["Split into narrow single-purpose tools","Add more few-shot examples","Move logic into the system prompt","Use a larger embedding model"], answer: 0, why: "Small, single-purpose tools with obvious contracts are far easier for a model to call correctly than one mega-tool with tangled optional arguments." },
  { q: "Tool returns a 200 KB JSON blob that blows the agent's context", options: ["Truncate and paginate tool outputs","Raise the context window","Lower the temperature","Cache the tool result"], answer: 0, why: "Tool results should return only what the next step needs, with pagination or summaries for the rest; a bigger window just delays the overflow and multiplies cost." },
  { q: "Users stare at a spinner for twenty seconds before any text appears", options: ["Prompt caching","Streaming tokens to the client","Quantizing the model","Retrying failed requests"], answer: 1, why: "Streaming shows tokens as they generate, cutting perceived latency to time-to-first-token; total generation time is unchanged but the wait feels instant." },
  { q: "Tail latency is dominated by a handful of very long responses", options: ["Cap max output tokens","Add a reranker","Increase batch size","Warm up the KV cache"], answer: 0, why: "Generation time scales with output length, so capping <code>max_tokens</code> (and prompting for brevity) directly bounds p99 latency; batching helps throughput, not a single request's tail." },
  { q: "Keyword search for one term misses documents using its synonym", options: ["BM25 tuning","Embedding-based semantic search","Regex expansion","Stemming"], answer: 1, why: "Embeddings place synonymous phrases near each other in vector space, so meaning matches even when words differ; stemming only normalizes word endings, not meaning." },
  { q: "The answer straddles a chunk boundary so retrieval returns half a fact", options: ["Chunk overlap and boundary-aware splitting","A larger embedding model","Lower similarity threshold","More retrieval candidates"], answer: 0, why: "Overlapping, structure-aware chunking keeps facts intact within a chunk; a fancier embedding model cannot recover text that was split apart before indexing." },
  { q: "Vector search whiffs on exact part numbers and error codes", options: ["Hybrid search combining BM25 and vectors","Bigger chunks","Higher temperature","Query expansion only"], answer: 0, why: "Embeddings blur rare exact strings like SKUs, while lexical BM25 nails them; hybrid search fuses both so neither semantic nor exact-match queries fail." },
  { q: "Correct passage is in the top fifty candidates but never the top five", options: ["Cross-encoder reranker","Larger chunk size","Lower top-k","Different distance metric"], answer: 0, why: "A cross-encoder reranker scores each query-passage pair jointly and reorders candidates far more accurately than the first-stage retriever; lowering top-k would discard the answer entirely." },
  { q: "Assistant fabricates plausible details about internal docs it has never seen", options: ["Retrieval-augmented generation","Higher temperature","Longer system prompt","Fine-tuning on public data"], answer: 0, why: "RAG retrieves the actual private documents into context so answers are grounded in real text; without retrieval the model can only hallucinate what it was never trained on." },
  { q: "Vague follow-up question like referring back to an earlier item retrieves garbage", options: ["Query rewriting with conversation context","Bigger vector index","Higher top-k","A stricter system prompt"], answer: 0, why: "Rewriting the query into a standalone, self-contained question before retrieval restores the missing referents; retrieving more of the wrong thing does not help." },
  { q: "Retrieval surfaces last year's policy doc next to this year's and the model blends them", options: ["Metadata filtering and recency ranking","More chunk overlap","Higher temperature","A larger context window"], answer: 0, why: "Filtering or boosting by document metadata such as version and date keeps stale sources out of context; the model cannot reliably arbitrate contradictions on its own." },
  { q: "Fact buried mid-context is ignored but works when placed at the start or end", options: ["Lost-in-the-middle position effect","Tokenizer mismatch","KV cache eviction","Embedding drift"], answer: 0, why: "Attention reliably favors the beginning and end of long contexts, so critical instructions and facts belong at the edges; this is a position effect, not a caching bug." },
  { q: "Hundred-turn chat degrades and an early key instruction is forgotten", options: ["Context compaction and summarization","Increase top-p","Add a reranker","Switch embedding models"], answer: 0, why: "Compacting old turns into a summary and re-pinning key instructions keeps the context within budget and salient; sampling parameters cannot rescue a drowned instruction." },
  { q: "Assistant forgets user preferences the moment a new session starts", options: ["Persistent memory store outside the context","Longer max tokens","Prompt caching","Chain-of-thought prompting"], answer: 0, why: "Context vanishes with the session, so durable preferences must live in an external memory store that is loaded on session start; caching only speeds up repeated prefixes." },
  { q: "Agent re-reads the same file forever and never finishes", options: ["Step budgets and loop detection","Bigger context window","Lower temperature","More tools"], answer: 0, why: "A hard step budget plus detection of repeated identical actions forces the loop to terminate or escalate; more context just lets the agent loop longer before dying." },
  { q: "Agent declares the bug fixed without ever running the tests", options: ["Require verifiable evidence before completion","Raise the step budget","Add chain-of-thought","Use a faster model"], answer: 0, why: "Agents need a verification gate, such as passing tests or checked outputs, before claiming success; unverified self-reports are a classic agent failure mode." },
  { q: "One agent's context overflows juggling research, coding, and review at once", options: ["Orchestrator with focused subagents","Increase max tokens","Semantic caching","Prompt compression only"], answer: 0, why: "Splitting work across subagents gives each a clean, focused context and lets an orchestrator merge results; one shared window forces every concern to compete for attention." },
  { q: "Subagent finishes but the orchestrator loses the details of what it did", options: ["Shared artifacts and structured handoff summaries","Longer subagent context","Lower temperature","Retry the subagent"], answer: 0, why: "Subagents should write results to shared artifacts or return structured summaries, since the parent never sees their internal transcript; retrying loses the same information again." },
  { q: "Standard way to plug the same tool servers into any client app", options: ["Model Context Protocol","REST with OpenAPI","Function calling","Webhooks"], answer: 0, why: "MCP standardizes how clients discover and call external tool servers, so one integration works across hosts; plain function calling is per-app wiring with no shared protocol." },
  { q: "Agent-generated shell commands could wreck the production host", options: ["Sandboxed execution environment","Lower temperature","Better system prompt","Output length limits"], answer: 0, why: "Untrusted generated code must run in an isolated sandbox with restricted filesystem and network access; prompting reduces but never guarantees safe commands." },
  { q: "A scraped webpage contains hidden instructions the agent then obeys", options: ["Indirect prompt injection","Jailbreak via user prompt","Data drift","Hallucination"], answer: 0, why: "Instructions smuggled inside retrieved or fetched content are <strong>indirect prompt injection</strong>: the attack rides in through data, not through the user's own message." },
  { q: "Limit the damage an agent can do while reading untrusted email", options: ["Least-privilege tools plus approval for sensitive actions","A politeness clause in the system prompt","Higher quality embeddings","Streaming output"], answer: 0, why: "Because injection cannot be fully prevented, the defense is capability control: minimal tool permissions and human confirmation on irreversible actions bound the blast radius." },
  { q: "Prompt tweak fixed one bug and silently broke three other cases", options: ["Eval regression suite as a shipping gate","Manual spot checks","Higher temperature","Version pinning"], answer: 0, why: "A regression eval run over a fixed test set before every prompt change catches silent breakage; spot-checking a few examples is how the three breaks slipped through." },
  { q: "Grade a hundred thousand free-text answers without an army of humans", options: ["LLM-as-judge with a rubric","Exact string match","BLEU score","User surveys"], answer: 0, why: "An LLM judge scoring against an explicit rubric scales open-ended evaluation; string match and BLEU collapse on free-form answers that are correct but worded differently." },
  { q: "Swapping the order of two candidate answers flips the judge's verdict", options: ["Position bias","Length bias","Self-preference bias","Rubric ambiguity"], answer: 0, why: "Judges systematically favor one position, often the second answer shown, so pairwise evals must randomize or average over both orders; this is order sensitivity, not verbosity preference." },
  { q: "Judge scores padded, rambling answers higher than crisp correct ones", options: ["Verbosity or length bias","Position bias","Recency bias","Anchoring on the first answer"], answer: 0, why: "Length bias makes judges reward longer outputs regardless of quality; rubrics must explicitly instruct that concision is not a defect and length is not evidence." },
  { q: "Judge model rates its own family's outputs suspiciously well", options: ["Self-preference bias, use a different judge","Position bias","Temperature too high","Overfitting to the rubric"], answer: 0, why: "Models tend to prefer text in their own style, so judging with the same model that generated the answers inflates scores; use an independent judge or a panel." },
  { q: "Production agent failed somewhere in a forty-step run and nobody knows where", options: ["Tracing with per-step spans","More retries","A longer system prompt","Higher rate limits"], answer: 0, why: "Traces record every model call, tool call, and intermediate output as spans, so you can replay the run and pinpoint the failing step; retrying blindly hides the cause." },
  { q: "Quality quietly dropped after the provider updated the hosted model", options: ["Continuous evals on live traffic plus version pinning","One-time launch benchmark","Bigger prompts","Prompt caching"], answer: 0, why: "Scheduled evals against a fixed suite detect silent model drift, and pinning a model version controls when you absorb changes; a launch-day benchmark protects nothing afterward." },
  { q: "Bot must always answer in a strict house style that prompt examples never quite nail", options: ["Fine-tuning on style exemplars","Retrieval-augmented generation","Bigger context window","Lower temperature"], answer: 0, why: "Fine-tuning excels at baking in consistent style, tone, and format; RAG injects facts, not behavior, and few-shot examples keep drifting on edge cases." },
  { q: "Model must reflect a product catalog that changes every night", options: ["RAG over the live catalog","Nightly fine-tuning","A longer system prompt","Higher top-k sampling"], answer: 0, why: "Fresh, changing facts belong in retrieval, where an index update takes minutes; fine-tuning bakes knowledge into weights that are stale by morning and expensive to refresh." },
  { q: "Fine-tune a huge model on a single modest GPU", options: ["LoRA adapters","Full-parameter fine-tuning","Distillation from scratch","Gradient checkpointing alone"], answer: 0, why: "LoRA trains small low-rank adapter matrices while freezing base weights, slashing memory needs; full fine-tuning of every parameter needs a multi-GPU cluster." },
  { q: "Model weights will not fit in the GPU memory you actually have", options: ["Quantization to lower precision","Increase batch size","Prompt compression","Speculative decoding"], answer: 0, why: "Quantizing weights to 8-bit or 4-bit shrinks the memory footprint several-fold with modest quality loss; batching and decoding tricks change speed, not model size." },
  { q: "Without this, each new token would recompute attention over every previous token", options: ["KV cache","Prompt cache","Semantic cache","Embedding cache"], answer: 0, why: "The KV cache stores per-token attention keys and values so each generation step only computes the new token; a prompt cache reuses a prefix across requests, not within decoding." },
  { q: "Self-hosted GPU sits mostly idle while serving requests one at a time", options: ["Continuous batching","Quantization","Smaller model","More replicas"], answer: 0, why: "Continuous batching interleaves many requests' decode steps on the same GPU, multiplying throughput; quantization and extra replicas do not fix the utilization of each one." },
  { q: "Most queries are trivial FAQs but every one pays flagship-model prices", options: ["Model routing by difficulty","Prompt caching","Fine-tuning the flagship","Streaming"], answer: 0, why: "A router sends easy queries to a cheap small model and escalates only the hard ones, often cutting cost around 10x; caching helps repeated prefixes, not per-query model choice." },
  { q: "A traffic burst triggers a wall of 429 responses and the pipeline collapses", options: ["Exponential backoff with jitter and client-side throttling","Immediate tight-loop retries","Switch to a bigger model","Longer timeouts"], answer: 0, why: "Backoff with jitter spreads retries and honors rate limits, while client-side queuing smooths bursts; hammering retries in a tight loop amplifies the very 429s that caused the failure." },
  { q: "Users paste national ID numbers that would flow into a third-party model API", options: ["PII detection and redaction before the call","Ask the model to ignore them","Log scrubbing afterward","Lower temperature"], answer: 0, why: "A guardrail layer must detect and mask PII before the request leaves your boundary; once sent, the data is already in someone else's logs no matter what the prompt says." }
]);
